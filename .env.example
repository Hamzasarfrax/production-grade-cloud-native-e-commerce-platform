# Root .env — sirf docker-compose.yml interpolation ke liye.
# Values backend/.env (Laravel) se match honi chahiye.
# Use: cp .env.example .env  (phir docker compose up)

DB_DATABASE=mxmobilz_db
DB_USERNAME=mxmobilz
DB_PASSWORD=mxmobilzsecret
DB_ROOT_PASSWORD=change-me-root
